import React from 'react';

const FullScreenContainer = (props) => {
    return (
        <React.Fragment>

            {props.children}

        </React.Fragment>
    );
};

export default FullScreenContainer;